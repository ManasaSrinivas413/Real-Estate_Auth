# Real-Estate-Auth

A simple user authentication system built with Django supporting signup, login, and role-based users (`Buyer`, `Seller`, `Agent`).  
The project uses MySQL (or SQLite) as the database backend and demonstrates secure password hashing, form validation, and Bootstrap-styled templates.

---

## Features

- **User Signup**
  - Users can create accounts with `username`, `email`, `phone`, `role`, and password.
  - Password confirmation validation.
  - Passwords are securely hashed before saving to the database.

- **User Login**
  - Users can login with `username` and `password`.
  - Role-based welcome message displayed after login.
  - Error messages for invalid credentials.

- **Roles**
  - Supports three roles: `Buyer`, `Seller`, `Agent`.
  - Roles can be extended in the model if needed.

- **Frontend**
  - Bootstrap-style form inputs with Font Awesome icons.
  - Field-level error messages.
  - Global success/error messages displayed.

---
## Project Structure

login_signup/ # Root Django project folder
├── login_signup/ # Project settings folder
│ ├── init.py
│ ├── settings.py # Django settings (databases, installed apps, etc.)
│ ├── urls.py # Project-level URLs
│ ├── asgi.py
│ └── wsgi.py
│
├── ls1/ # Custom app for signup/login functionality
│ ├── init.py
│ ├── admin.py # Register models for admin panel
│ ├── apps.py
│ ├── forms.py # SignupForm, LoginForm
│ ├── models.py # SuperUser model (or custom user model)
│ ├── views.py # signup_view, login_view
│ ├── urls.py # App-specific URLs
│ ├── migrations/ # Auto-generated by Django
│ │ ├── init.py
│ │ └── 0001_initial.py
│ └── templates/ # HTML templates
│ ├── signup.html
│ └── login.html
│
├── static/ # Static files (CSS, JS, images)
│ └── css/
│ └── style.css
│
├── db.sqlite3 # SQLite database file (or MySQL configured in settings)
├── manage.py # Django CLI utility
---

# Setup & Installation for Real-Estate-Auth Django Project

# 1. Create a virtual environment
python -m venv venv

# Activate environment
Windows
venv\Scripts\activate

# Install Django
pip install django

# 2. Create a Django Project
django-admin startproject login_signup

# 3. Create a Django App
cd login_signup
python manage.py startapp ls1

# 4. Configure the database
# In login_signup/settings.py, add:

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',  # or 'django.db.backends.sqlite3' for SQLite
        'NAME': 'myapp',
        'USER': 'root',
        'PASSWORD': 'root',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}

# 5. Create the database in MySQL
Open MySQL shell and run:
CREATE DATABASE myapp;

# 6. Apply migrations
python manage.py makemigrations
python manage.py migrate

# 7. Run the development server
python manage.py runserver

# Database Operations (MySQL / SQLite)
 Show all databases
SHOW DATABASES;

# Use the project database
USE myapp;

# Show all tables
SHOW TABLES;

# Inspect all users in the superuser table
SELECT * FROM ls1_superuser;

# Usage Instructions

# Signup
Go to /signup/
Fill in username, email, phone, role, password, and confirm password
Submit to create an account

# Login
Go to /login/
Enter username and password
After successful login, you’ll see a welcome message with your role
Error Handling
Field-specific errors are shown below each input
Global messages displayed at the top

# Security Notes
 Passwords are hashed using Django’s built-in authentication system
 confirm_password is not stored in the database, only used for form validation
 Authentication uses Django’s authenticate and login functions for safety

# Customization
 Roles: Modify ROLE_CHOICES in models.py to add or remove user roles
 Styling: Update static/css/style.css for custom styles
 Icons: Update Font Awesome icons in templates
